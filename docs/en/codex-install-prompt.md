# Installation prompt compatibility page

The former product-specific installation prompt has moved to the
[generic coding-agent installation prompt](agent-install-prompt.md).

Use the linked document as the single maintained source. This compatibility
page intentionally does not duplicate the prompt, so existing v0.1.2 links
continue to work without maintaining two implementations.
